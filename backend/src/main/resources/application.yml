spring:
  application:
    name: hospital-api
  datasource:
    url: jdbc:postgresql://localhost:5432/hospital
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        default_schema: hospital
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8080

app:
  security:
    enabled: true
  jwt:
    # Mínimo 32 bytes UTF-8 para HS256; sustituir en producción (variable de entorno recomendada).
    secret: "desarrollo-local-clave-muy-larga-de-al-menos-32-bytes!!"
    expiration-minutes: 120
  cors:
    allowed-origins:
      - http://localhost:4200

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
